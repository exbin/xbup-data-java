group = 'org.exbin.xbup.data'
ext.libraryName = 'XBUP Math Library'
ext.libraryDescription = 'Library for mathematical data types with XBUP support'

apply from: '../xbup-data-java-modules.gradle'

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile moduleDep('XbupJava', 'org.exbin.xbup', 'xbup-core')
    testCompile group: 'junit', name: 'junit', version: '4.10'    
}
